source 'https://rubygems.org'

gem 'rails', '4.2.3'                                      # Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'pg'                                                  # Use postgresql as the database for Active Record
gem 'sass-rails', '~> 5.0'                                # Use SCSS for stylesheets
gem 'uglifier', '>= 1.3.0'                                # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.1.0'                            # Use CoffeeScript for .coffee assets and views
gem 'jquery-rails'                                        # Use jquery as the JavaScript library
gem 'turbolinks'                                          # Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'jbuilder', '~> 2.0'                                  # Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder

# Custom Gems
gem 'haml'                                                # Haml Templates Engine
gem 'haml-rails'                                          # Haml-rails provides Haml generators for Rails 4
gem 'puma'                                                # Thin Ruby WebServer Support
gem 'bootstrap-sass'                                      # bootstrap-sass is a Sass-powered version of Bootstrap, ready to drop right into your Sass powered applications.
gem 'database_cleaner'                                    # database clean library
gem 'faker'                                               # The faker and ffaker APIs are mostly the same, although the API on ffaker keeps growing with its users additions.
gem 'factory_girl_rails'                                  # factory_girl is a fixtures replacement with a straightforward definition syntax, support for multiple build strategies
gem 'money-rails'                                         # This library provides integration of money gem with Rails.
gem 'kaminari'                                            # A Scope & Engine based, clean, powerful, customizable and sophisticated paginator for Rails 4
gem 'kaminari-bootstrap'                                  # Basic Gem for quick default inclusion of Kaminari theme compatible with Twitter Bootstrap 2.0

group :development, :test do
  gem 'byebug'                                            # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'web-console', '~> 2.0'                             # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'spring'                                            # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'rspec-rails'                                     #rspec-rails is a testing framework for Rails 3.x and 4.x.
end

# Custom Gems
# gem 'jquery-ui-rails'                                     # jQuery UI's JavaScript, CSS, and image files packaged for the Rails 3.1+ asset pipeline
# gem 'jquery-turbolinks'                                   # It's necessary for Turbolinks.
# gem 'progress_bar'
# gem 'redis-rails'                                         # provides a full set of stores (Cache, Session, HTTP Cache) for Ruby on Rails.
# gem 'redis-objects'                                       # This gem provides a Rubyish interface to Redis, by mapping Redis data types to Ruby objects, via a thin layer over the redis gem.
# gem 'rails_config'                                        # RailsConfig helps you easily manage environment specific Rails settings in an easy and usable manner
# gem 'simple_form'                                         # Simple Form aims to be as flexible as possible while helping you with powerful components to create your forms. The basic goal of Simple Form is to not touch your way of defining the layout, letting you find the better design for your eyes. Most of the DSL was inherited from Formtastic, which we are thankful for and should make you feel right at home.
# gem 'underscore-rails'                                    # asset-pipeline gem to provide underscore.js
# gem 'faye'                                                # WebSocket Server command: bundle exec puma faye.ru -p 9292 -e production
# gem 'kaminari'                                            # A Scope & Engine based, clean, powerful, customizable and sophisticated paginator for Rails 4
# gem 'kaminari-bootstrap'                                  # Basic Gem for quick default inclusion of Kaminari theme compatible with Twitter Bootstrap 2.0
# gem 'faker'                                               # The faker and ffaker APIs are mostly the same, although the API on ffaker keeps growing with its users additions.
# gem 'factory_girl_rails'                                  # factory_girl is a fixtures replacement with a straightforward definition syntax, support for multiple build strategies
# gem 'database_cleaner'                                    # database clean library
# gem 'foreigner'                                           # Foreigner introduces a few methods to your migrations for adding and removing foreign key constraints. It also dumps foreign keys to schema.rb.
# gem 'jquery-datatables-rails'                             # The current gem is released without updated plugins. If you encounter any errors, please fork the repository, update the plugin files and send a pull-request.
# gem 'chosen-rails'                                        # Chosen is a library for making long, unwieldy select boxes more user friendly.
# gem 'rails3-jquery-autocomplete'                          # An easy way to use jQuery's autocomplete with Rails 3.
# gem 'cocoon'                                              # Cocoon makes it easier to handle nested forms.
# gem 'whenever', :require => false                         # Whenever is a Ruby gem that provides a clear syntax for writing and deploying cron jobs.
# gem 'ejs'                                                 # EJS templates for JavaScript
# gem 'gritter'                                             # This Ruby on Rails gem allows you to easily add Growl-like notifications to your application using a jQuery plugin called 'gritter'.
# gem 'andand'                                              # If the result is nil, then the expression returns nil without throwing a NoMethodError.
# gem 'inherited_resources'                                 # Inherited Resources speeds up development by making your controllers inherit all restful actions so you just have to focus on what is important. It makes your controllers more powerful and cleaner at the same time.
# gem 'ajax-datatables-rails'                               # It is a wrapper around datatable's ajax methods that allow synchronization with server-side pagination in a rails app.

group :development do
  # gem 'better_errors'                                     # More Flexible Errors Pages
  # gem 'quiet_assets'                                      # Disable Assets Logger
  # gem 'pry-rails'                                         # More Flexible Rails Console
  # gem 'jazz_hands',
  #     :github => 'nixme/jazz_hands',
  #     :branch => 'bring-your-own-debugger'                # Spending hours in the rails console? Spruce it up and show off those hard-working hands!
  # gem 'rails_best_practices'
end

group :test do
  # gem 'rspec-rails'                             #rspec-rails is a testing framework for Rails 3.x and 4.x.
end
